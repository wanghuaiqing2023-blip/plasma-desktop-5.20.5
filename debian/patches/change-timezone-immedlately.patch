Description: <short summary of the patch>
 TODO: Put a short summary on the line above and replace this paragraph
 with a longer explanation of this change. Complete the meta-information
 with other relevant fields (see below for details). To make it easier, the
 information below has been extracted from the changelog. Adjust it or drop
 it.
 .
 plasma-desktop (4:5.20.5-4+deb11u1.lnd.35) unstable; urgency=medium
 .
   * change timezone immediately
Author: Loongson <service@loongnix.cn>

---
The information above should follow the Patch Tagging Guidelines, please
checkout http://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: <vendor|upstream|other>, <url of original patch>
Bug: <url in upstream bugtracker>
Bug-Debian: https://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: <no|not-needed|url proving that it has been forwarded>
Reviewed-By: <name and email of someone who approved the patch>
Last-Update: 2025-02-24

--- plasma-desktop-5.20.5.orig/kcms/dateandtime/dtime.cpp
+++ plasma-desktop-5.20.5/kcms/dateandtime/dtime.cpp
@@ -125,7 +125,11 @@ Dtime::Dtime(QWidget * parent, bool have
 
 void Dtime::currentZone()
 {
-    KTimeZone localZone = KSystemTimeZones::local();
+//    KTimeZone localZone = KSystemTimeZones::local();
+    KTimeZone localZone = KSystemTimeZones::zone(m_currentTimeZone);
+    if (!localZone.isValid()) {
+        localZone = KSystemTimeZones::local();
+    }
 
     if (localZone.abbreviations().isEmpty()) {
         m_local->setText(i18nc("%1 is name of time zone", "Current local time zone: %1",
@@ -227,6 +231,7 @@ void Dtime::load()
         currentTimeZone  = KSystemTimeZones::local().name();
   }
 
+  m_currentTimeZone = currentTimeZone;
   // Reset to the current date and time
   time = QTime::currentTime();
   date = QDate::currentDate();
@@ -241,6 +246,8 @@ void Dtime::load()
   currentZone();
 
   tzonelist->setSelected(currentTimeZone, true);
+  tzonelist->update();
+  m_local->update();
   emit timeChanged(false);
 }
 
--- plasma-desktop-5.20.5.orig/kcms/dateandtime/dtime.h
+++ plasma-desktop-5.20.5/kcms/dateandtime/dtime.h
@@ -86,6 +86,8 @@ private:
   bool		refresh;
   bool		ontimeout;
   bool  m_haveTimedated;
+
+  QString m_currentTimeZone;
 };
 
 class Kclock : public QWidget
